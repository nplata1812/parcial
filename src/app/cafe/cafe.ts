

export class Cafe {
  id: number;
  nombre: string;
  tipo: string;
  region: string;
  sabor: any;
  altura: number;
  imagen: string;

  constructor(
    id: number,
    nombre: string,
    tipo: string,
    region: string,
    sabor: any,
    altura: number,
    imagen: string,
  ) {
    (this.id = id);
      (this.nombre = nombre);
      (this.id = id);
      (this.tipo = tipo);
      (this.region = region);
      (this.sabor = sabor);
      (this.altura = altura);
      (this.imagen = imagen);
  }
}
